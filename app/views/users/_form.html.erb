<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="item">
    <%= f.label :name %>
    <%= f.text_field :name, :class => 'basic_input' %>
  </div>
  <div class="item">
    <%= f.label :email %>
    <%= f.text_field :email, :class => 'basic_input' %>
  </div>
  <div class="item">
    <%= f.label :pwd %>
    <%= f.text_field :pwd, :class => 'basic_input'%>
  </div>
  <div class="item">
    <label>&nbsp;</label>
    <%= f.submit :class => 'form_submit' %>
  </div>
<% end %>
